const puppeteer = require('puppeteer');
const fs = require('fs');

class webScrapper {
    constructor() {
        this.url = ''
        this.outputFile = ''
    }

    async initialise() {

    }

    async scrape() {
        return data
    }

    async run() {
        await this.initialise()
        await this.scrape()
    }
}

// TDD
// Commander.js with argv